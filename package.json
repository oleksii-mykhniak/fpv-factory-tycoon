{
  "name": "fpv-factory-tycoon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "android": "vite build && npx cap sync android && npx cap run android",
    "android:sync": "vite build && npx cap sync android",
    "android:debug": "vite build --mode debug && npx cap sync android && npx cap run android"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "excalibur": "^0.32.0"
  }
}
